# DFM Mimir VERSION (build BUILD)

<!-- Maintainer template: replace all placeholders and instructions before publishing.
Describe the actual shipped release, not planned capabilities or only its changes. -->

A short description of the app, bundled model and offline/account requirements.

## Downloads and installation

Direct links, platform/architecture and OS requirements, actual included backends,
installation and upgrade steps, signing status, model extraction/disk space,
startup/recovery instructions and unsupported distribution targets.

## Chat, context and models

Saved conversations and generation controls; actual context/reply defaults,
training/configurable limits, memory behavior, compaction and summary controls,
approximate inference modes and their defaults. Model selection/import/download
instructions and supported formats, catalog/discovery rules and trust labels,
network consent and offline behavior. State unavailable capabilities explicitly.

## OpenAI-compatible API

Supported platforms; enable/disable/restart behavior and UI labels; base URL,
model alias, port/address configuration, authentication and network exposure;
copyable request example; endpoints, streaming/sampling, supported message roles,
unsupported features, history/compaction behavior, resource-sharing and errors.

## Headless use

Copyable commands per supported platform, executable paths, required bundled
files, model/profile selection, context/device/port options, authentication,
shutdown and memory implications of running separate GUI/server processes.

## Optional feedback and privacy

What stays local, what is sent and when, separate network/submission/publication
consent and defaults, attribution options, failure/cancellation behavior,
provider/storage scope, administrative access separation and contact.

## Changes and model provenance

Version-specific changes, fixes and known regressions. Exact model repository,
revision, file/precision/size/hash, licenses and package source commit. Changes
supplement the complete usage instructions above rather than replacing them.

## Validation and package checksums

Evidence specific to these artifacts and their limitations. Distinguish successful
builds/unit tests from real-model or device qualification. Include all package
SHA-256 values, sidecars and links to pinned supporting documentation.
