DFM Mimir **0.1.5 (build 7)** is a local chat app with the official **Mimir v1.5 Q4_K_M (4-bit)** model included. No account, model download or model import is required to start chatting. Desktop packages also include an optional OpenAI-compatible local API and a standalone headless server.

## Downloads and installation

| Platform | Download | Inference |
|---|---|---|
| macOS 14+ Apple Silicon | [DMG](https://github.com/schneiderkamplab/HRM-Text/releases/download/dfm-mimir-v0.1.5/dfm-mimir-0.1.5-macos-arm64.dmg) | Metal acceleration with CPU fallback |
| Android ARM64 | [APK](https://github.com/schneiderkamplab/HRM-Text/releases/download/dfm-mimir-v0.1.5/dfm-mimir-0.1.5-android-arm64.apk) | CPU by default; experimental Vulkan is opt-in |
| Linux x64 | [tar.gz](https://github.com/schneiderkamplab/HRM-Text/releases/download/dfm-mimir-v0.1.5/dfm-mimir-0.1.5-linux-x64.tar.gz) | CPU; GTK3 desktop required, built on Ubuntu 22.04 |
| Windows x64 | [ZIP](https://github.com/schneiderkamplab/HRM-Text/releases/download/dfm-mimir-v0.1.5/dfm-mimir-0.1.5-windows-x64.zip) | CPU; release VC runtime included |

- **macOS:** mount the DMG, drag **DFM Mimir.app** into Applications, then open it there and eject the image.
- **Linux/Windows:** extract the entire archive and run `dfm-mimir` / `dfm-mimir.exe`. Keep the libraries, assets and server executable together.
- **Android:** install the APK. Open **Model and settings**, choose limits/backend, then press **Load model**. Startup deliberately leaves the model unloaded so settings remain accessible. Start with CPU, 1,024 context tokens and 512 reply tokens. After a freeze, force-stop the app and reopen it to change settings. Changing an initialized backend also requires force-stop/reopen.

Each package is roughly 1.1–1.2 GB because it includes the model. Download size is not RAM use: model loading and longer contexts require additional memory. Android also extracts a model copy into app-private storage, so allow space for both the APK and extracted model, plus a temporary copy during updates. Each download has a matching `.sha256` sidecar in Assets.

These packages retain development signing: macOS is ad-hoc signed and not notarized; Android uses the development key; Windows is unsigned. If macOS blocks the app, use the per-app **Open Anyway** option in System Settings → Privacy & Security only if you trust the download. Physical Android Vulkan remains unqualified. Linux/Windows packages do not include CUDA or Vulkan acceleration. No distributable iOS package is included. An unsigned iOS 17+ archive has been validated locally; TestFlight requires Apple Developer signing and upload. See the [TestFlight handoff](https://github.com/schneiderkamplab/HRM-Text/blob/dfm-mimir-v0.1.5/native/app/TESTFLIGHT.md).

## Chat, context and model selection

The app provides saved conversations, streaming replies, stop, sharing, model/backend settings and a context-use indicator. **Model and settings** controls context length and reply budget. Desktop automatic sizing uses available memory; Android starts with conservative manual limits. The bundled model was trained at **4,096 tokens**; configurable contexts up to **32,768** are experimental and may reduce answer quality or exceed available memory.

**Automatically compact context** summarizes older turns when context fills, aiming to free at least half the context when the current prompt and reply reservation permit. Oversized prompts or historical turns are split into bounded pieces for summarization. Original messages remain in the transcript; shortened text is kept separately for inference. **Show compaction summary in chat** reveals summaries where they occurred, including while they stream. Compaction is lossy and can take multiple model passes. Disabling it restores original text for inference, which may exceed context.

**MixedLM mode** is an optional approximation that reuses representations of older turns to reduce repeated prompt processing. It is on by default for new settings; a saved choice is preserved. It can change answers. Turn it off for exact PrefixLM.

**Temperature** (0–2, default 0) and **Repetition penalty** (1–2, default 1.1) are adjustable in settings and saved. Higher temperature adds variety; 0 uses greedy decoding. The repetition penalty discourages the last 64 generated tokens within each reply; 1 disables it. The former saved 1.0 default upgrades once to 1.1, while other customized values remain. These controls do not override API request sampling or compaction's deterministic defaults.

Assistant replies and visible summaries render basic Markdown, including headings, emphasis, lists, quotes and code blocks, even while streaming. User messages remain literal; stored/shared text is unchanged. Images render as text placeholders. Links open an inspect/copy dialog, without network access.

In **Model and settings → Choose or download a model…**, view the selected model, download sizes/status and installed models; select another model or import a local GGUF. Enable **Allow model downloads and discovery**, then **Check for newer models**, to refresh the catalog. Networking is optional and off by default; it is not needed for bundled chat.

The curated catalog and automatic discovery cover official `danish-foundation-models` repositories whose names contain both `mimir` and `gguf`, case-insensitively. Official corrected Q4_K_M, Q8_0 and BF16 exports are listed. Under **Additional Hugging Face repositories → +**, add an exact public `owner/repository` ID, such as `noctrex/DFM-Mimir`, to include third-party GGUFs. Adding an ID does not enable networking. Removing it stops its listings/download access and preserves already installed files.

Downloads are pinned to repository revisions and checked against size/SHA-256 metadata. Discovered models are unverified and use conservative fallback settings; arbitrary architectures are not guaranteed to work. Split GGUFs and private/gated authentication are not supported. Model refresh/download contacts GitHub and Hugging Face; it does not send chats.

## OpenAI-compatible API on desktop

**macOS, Linux and Windows** expose an optional **text Chat Completions subset** of the OpenAI API. Android does not expose this API.

1. Load a model, open **Model and settings**, choose the port (default `8080`) and enable **OpenAI-compatible local API**.
2. The active address appears in settings. Configure your client with base URL **`http://127.0.0.1:8080/v1`** and model **`dfm-mimir`**.
3. The API starts off by default; enable it again after restarting the app. Turning it off cancels API requests. Closing the app stops it.

The port is configurable while the API is stopped. The bind address is fixed to **127.0.0.1**, so this version accepts local callers only; LAN listening and browser-origin/CORS requests are not supported. Set environment variable `MIMIR_API_KEY` before launching the app/server to require `Authorization: Bearer YOUR_KEY`. Otherwise no API key is required; clients that insist on one can use a placeholder. No OpenAI account is needed.

Supported endpoints are `GET /v1/models` and `POST /v1/chat/completions`. For example, after starting the API:

```sh
curl http://127.0.0.1:8080/v1/models
curl http://127.0.0.1:8080/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"dfm-mimir","messages":[{"role":"user","content":"Hej Mimir!"}],"max_tokens":64}'
```

The example uses POSIX shell quoting. Responses can be ordinary JSON or SSE with `"stream":true`. Supported sampling controls include `temperature`, `top_p`, `seed`, and one token budget (`max_tokens` or `max_completion_tokens`). Messages must contain text: an optional initial `system` message, then alternating `user`/`assistant` turns ending in `user`. Mimir's own chat template is applied.

API requests share the UI's loaded model, execute serially, and do not create or modify saved chats. Clients must supply conversation history. API requests do **not** use automatic compaction or cross-request MixedLM reuse; oversized requests return an error. Tools/function calling, images/audio, developer-role messages, structured response formats and the Responses API are unsupported. This is not full OpenAI API parity. See the [API guide](https://github.com/schneiderkamplab/HRM-Text/blob/dfm-mimir-v0.1.5/native/app/API.md) for the complete field/error/queue limits.

## Headless desktop server

No GUI or display server is required. The included server loads the packaged model and exposes the same API:

```sh
# Linux: run from the extracted package
./dfm-mimir-server --port 8080

# macOS: run from Terminal after installing the app
"/Applications/DFM Mimir.app/Contents/MacOS/dfm-mimir-server" --port 8080
```

```powershell
# Windows PowerShell: run from the extracted package
.\dfm-mimir-server.exe --port 8080
```

Use `--help` for options, including `--model FILE.gguf`, `--profile FILE.json`, `--device auto|cpu|DEVICE`, `--context TOKENS`, `--max-tokens TOKENS`, and `--port PORT`. Default context sizing is automatic and the default reply budget is 512 tokens. An imported model uses the bundled profile unless you supply another profile. Stop with **Ctrl-C**. A separately launched GUI and server each load their own model and consume additional memory. The same loopback-only binding and optional `MIMIR_API_KEY` authentication apply.

## Optional chat feedback and privacy

Thumbs up/down can share the selected conversation with the Mimir team to improve future Mimir and other DFM models. **Online feedback is off by default**, independently of model-download permission. The first thumb click asks whether to enable it; declining leaves local chat usable. Change this under **Model and settings → Allow online feedback**.

Every submission requires **Confirm and send** after a preview showing the exact chat, any compaction summary (including hidden summaries), optional comment, editable device-generated pseudonym and limited app/model/settings metadata. The separate **CC BY 4.0 publication permission defaults on** and can be unchecked. Enabling feedback alone sends nothing; cancelling sends nothing. Network denial or submission failure affects only feedback, not local chat.

Submissions go to the team's Cloudflare service. D1 database storage/execution uses EU jurisdiction; Worker request processing may occur elsewhere. Publication is a separate team review decision, not automatic on submission. The app contains no Cloudflare administrative credentials or feedback read/export access. Contact: **petersk@imada.sdu.dk**. OS networking permissions are declared; the consent switches are app-level controls, not a general OS Internet-permission prompt.

## Changes in 0.1.5 and model provenance

This version replaces the bundled model with **DFM Mimir v1.5 Q4_K_M** and adds its Q8_0 and BF16 variants to the curated catalog. Original Mimir downloads remain available. Existing users with the bundled model selected switch to the new bundled model; explicitly selected downloaded/imported models remain selected. Saved chats remain available. It retains UCloud branding (“Trained on SDU UCloud”), a concise default system prompt identifying Mimir and Danish Foundation Models as a research collaboration, reliable composer focus after replies, and optional model-initiated web search. It retains MixedLM, sampling controls, Markdown, corrected weights, model selection, compaction and the desktop API/headless server.

## Optional web search

In **Model and settings**, enable **Allow online search** and enter a Mimir search key. The key is masked with stars; the eye button reveals or hides it. You can remember it in platform secure storage or use it only for this session. No search key is bundled or required for offline chat.

When enabled and configured, Mimir receives a `web_search(query)` tool definition and can search while answering. The app shows a searching status, passes results back to the model, and allows at most two searches per answer. Stop cancels a search. There is no separate search button/dialog. Turning search off omits the tool definitions and prevents search requests.

Only search queries go to the Mimir Cloudflare service and Jina AI, but model-generated queries may contain chat details. The Jina provider key stays on the server; it is never sent to the app. Tool results are retained locally with the completed conversation. Feedback exports user/assistant text and compaction summaries, which may quote search results, but does not export raw tool transcripts or keys. Search failure does not disable offline chat. Search results and tool definitions consume context; small context settings may need increasing. Results may be inaccurate and the model may omit source URLs. This tool is available in the chat UI, not through the desktop OpenAI-compatible API.

Linux additionally needs `libsecret-1-0` and a Secret Service to remember keys; without secure storage, session-only search remains available.

Bundled file: [`danish-foundation-models/DFM-Mimir-v1.5-GGUF`](https://huggingface.co/danish-foundation-models/DFM-Mimir-v1.5-GGUF/tree/78f92c5f126ae7ad05e98fc210d5c9c0eec3da16), `dfm-mimir-v1.5-q4_k_m.gguf` (1,167,418,016 bytes).

Model SHA-256: `38ecdf6303394b256037287f2caf9b334e6a20814fd07d11bdc24555dc5d01e6`. Model and dependency licenses are included in the app.

Package source: [dfm-mimir-v0.1.5](https://github.com/schneiderkamplab/HRM-Text/tree/dfm-mimir-v0.1.5). [Model documentation](https://github.com/schneiderkamplab/HRM-Text/blob/dfm-mimir-v0.1.5/native/app/MODELS.md).

## Validation

All **67 app tests** and static analysis passed locally and in [Linux/Windows CI](https://github.com/schneiderkamplab/HRM-Text/actions/runs/35864441924). Regression tests cover catalog upgrades, removal of stale bundled-model inventory and preservation of downloaded models. CI also passed native fallback/compaction tests, packaged CPU backend discovery and missing-backend diagnostics.

The v1.5 GGUFs passed 724 tokenizer/decoder/template comparisons and 19 training-tokenizer audit cases per precision, plus CPU/Metal chat smoke tests. With the bundled Q4_K_M, the native Metal runtime passed search-tool continuation (offline synthetic results), streaming, cancellation/recovery, compaction, MixedLM cache lifecycle and shutdown checks. The mounted macOS DMG passed real-model API tests for normal/streaming replies, seeded sampling, concurrency, overflow rejection, disconnect recovery and shutdown.

All four packages contain the verified v1.5 Q4_K_M weights. Linux/Windows per-file manifests and source provenance, Linux executable permissions, Android signature/ARM64 ABI/version/build/16 KiB alignment, DMG integrity/signature and credential bundle audits passed. Private local search credentials were absent from the packages. Linux/Windows checks are not clean-host real-model or GPU qualification. Physical Android Vulkan remains unqualified.

The iOS Metal framework and unsigned **0.1.5 (7)** archive build successfully with the correct bundled model hash. This does not establish signed-device or App Store acceptance; no TestFlight upload has occurred. The handoff describes the remaining signing/account/device steps.

Package runtime source: `3e36860ef4d918591c64e0d789f104df5047eba4`; subsequent release commits update documentation only. **0.1.4 remains available with its original model and assets.**

## Package SHA-256 checksums

```

bda89309f2576aa11239d3ad2eb1e7dd961abc86d43baa4773e785648b716900  dfm-mimir-0.1.5-android-arm64.apk
22395649effebace0a3ec7d0283a9dcb3cf1fcc7df3d2453a6fddad2a6e0c5d3  dfm-mimir-0.1.5-linux-x64.tar.gz
33d0e3595e2825a8fe406d09778e3ddc2cb58bdf407755f80259f7efde7b6b9f  dfm-mimir-0.1.5-macos-arm64.dmg
f8697fc4a48a9ed409eb841e086176155cac8664155d8760ba178da89260b547  dfm-mimir-0.1.5-windows-x64.zip
```
